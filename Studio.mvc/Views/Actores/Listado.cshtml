@{
    ViewData["Title"] = "Actores Page";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="~/css/Listas.css"> <!-- Corregido -->
    <title>@ViewData["Title"]</title>
</head>
<body>
    <h1 class="display-4">Actores</h1>
    <h3>Listado</h3>

    <table class="ListadoTabla">
        <thead>
            <tr>
                <th>Perfil</th>
                <th>Actores</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach(var repo in Model)
            {
                <tr>
                    <td>
                        <img src="@(repo.IMG)" class="ImgPerfil" alt="Imagen de @repo.Nombre">
                    </td>
                    <td>
                        <a asp-action="Detalle" asp-route-Id="@repo.IdActor">
                            @repo.Nombre @repo.Apellido
                        </a>
                    </td>
                    <td>
                        <a asp-action="Modificar" asp-route-Id="@repo.IdActor">Modificar</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</body>
</html>
